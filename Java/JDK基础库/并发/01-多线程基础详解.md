---
tags: [多线程, Thread, Runnable, synchronized, wait, notify, 并发, Java]
创建日期: 2026-08-09
状态: ✅ 已归档（01-学习/Java/JDK基础库/并发）
归属: 01-学习/Java/JDK基础库/并发
---

# 01-多线程基础详解

> 版本基线：JDK 8+ 线程基础（Thread/Runnable/synchronized/wait/notify/interrupt）
> 受众：Java 后端开发，已懂进程/线程概念，要系统掌握 Java 多线程基础 API。默认你懂面向对象、异常处理。
> 关联笔记：[00-并发编程总览](00-并发编程总览.md)、[JMM内存模型详解](../../JVM/JMM内存模型详解.md)、[Java volatile详解](Java volatile详解.md)、[01-JUC之锁与AQS](JUC/01-JUC之锁与AQS.md)

## 📋 总纲

- 1. 并发核心概念（特征与状态）
- 2. 并发的问题（数据竞争/死锁/活锁等）
- 3. Thread 和 Runnable
- 4. Callable
- 5. Synchronized 关键字
- 6. wait 与 notify
- 7. interruptedException 与 interrupt
- 8. 优雅关闭线程

## 学习目标

学完本篇你能：

1. 说清并发三要素（原子性/有序性/可见性）与线程五状态
2. 识别并发问题：数据竞争/死锁/活锁/资源不足/优先权反转
3. 用 Thread/Runnable/Callable 三种方式创建线程
4. 正确使用 synchronized（三种方式/对象锁/类锁/可重入）
5. 理解 wait/notify 协作机制与为什么 wait 必须释放锁
6. 正确处理中断（interrupt/InterruptedException）
7. 实现线程的优雅关闭

## 前置知识

- 需掌握：Java 基础语法、面向对象

---

# Thread 和 Runnable
```java
public class MyThread  extends Thread{

    @Override
    public void run() {
        while (true){
            System.out.println(Thread.currentThread().getName()+"运行了.");
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }
}
```
```java
public static void main(String[] args) {
      new MyThread().start();
  }
```
---
```java
public class MyRunnable implements Runnable{
    @Override
    public void run() {
        while (true){
            System.out.println(Thread.currentThread().getName()+"运行了.");
            try {
                Thread.sleep(1000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
        }
    }
}
```
```java
  public static void main(String[] args) {
      new Thread(new MyRunnable()).start();
  }
```
---
Runnable 接口只定义了一个方法：run 方法
Thread 类其他常用方法：
- 获取或设置 Thread 对象信息的方法
  - getId
  - getName/setName
  - isDaemon/setDaemon
  - getPriority/setPriority
  - getState
- interrupt
- interrupted
  静态方法
判断目标线程是否被中断，但是将清除线程的中断标记
- isInterrupted
- sleep
- join
- setUncaughtExceptionHandler
- currentThread
### yield 方法
#### 什么是 CPU 执行权
操作系统是为每个线程分配一个时间片来占有 CPU 的，正常情况下当一个线程把分配给自己的时间片使用完后，线程调度器才会进行下一轮的线程调度，这里所说的 “自己占有的时间片” 即 CPU 分配给线程的执行权。
何为让出 CPU 执行权呢？
当一个线程通过某种可行的方式向操作系统提出让出 CPU 执行权时，就是在告诉线程调度器自己占有的时间片中还没有使用完的部分自己不想使用了，主动放弃剩余的时间片，并在合适的情况下，重新获取新的执行时间片。
#### 方法简介
Thread 类中有一个静态的 yield 方法，当一个线程调用 yield 方法时，实际就是在暗示线程调度器当前线程请求让出自己的 CPU 使用权。
```java
public static native void yield();
```
- native
  - 抽象地讲，一个 Native Method 就是一个 Java 调用的非 Java 代码的接口。一个 Native Method 是这样一个 Java 的方法：该方法的实现由非 java 语言实现。
  - 简单的来说，native 方法就是我们自己电脑的方法接口，比如 Windows 电脑会提供一个 yield 方法，Linux 系统的电脑也同样会提供一个 yield 方法，本地方法，可以理解为操作调用操作系统的方法接口。
作用
暂停当前正在执行的线程对象（及放弃当前拥有的 cup 资源），并执行其他线程。
### yield 和 sleep 区别
- 线程执行 sleep () 方法后转入阻塞 (blocked) 状态，而执行 yield () 方法后转入就绪 (ready) 状态
- sleep () 方法声明会抛出 InterruptedException, 而 yield () 方法没有声明任何异常
# Callable
Callable
- 接口
有简单类型参数，与
- 声明
- call()
```java
public class MyCallable implements Callable<String> {
    @Override
    public String call() throws Exception {
        Thread.sleep(4000);

        return "call返回值.";
    }
}
```
```java
public static void main(String[] args) throws ExecutionException, InterruptedException {
      MyCallable myCallable = new MyCallable();

      FutureTask<String> futureTask = new FutureTask<>(myCallable);

      new Thread(futureTask).start();

      // 同步等待返回值
      String str = futureTask.get();
      System.out.println(str);
  }
```
上面是异常没有校验的。下面：
```java
public static void main(String[] args) throws ExecutionException, InterruptedException {
    MyCallable myCallable = new MyCallable();
    ThreadPoolExecutor threadPoolExecutor = new ThreadPoolExecutor(
            5, 5, 1, TimeUnit.SECONDS,
            new ArrayBlockingQueue<>(10)
    ){
        @Override
        protected void afterExecute(Runnable r, Throwable t) {
            // 如果在 Call 执行过程中有异常，可以在这里处理
            // super.afterExecute(r, t);
            System.out.println("任务异常..."  + t);
        }
    };

    Future<String> stringFuture = threadPoolExecutor.submit(myCallable);

    // 同步等待返回值
    String str = stringFuture.get();
    System.out.println(str);

    threadPoolExecutor.shutdown();
}
```
# Synchronized 关键字
synchronized 同步块是 Java 提供的一种原子性内置锁，Java 中的每个对象都可以把它当作一个同步锁来使用，这些 Java 内置的使用者看不到的锁被称为内部锁，也叫作监视器锁。
线程的执行
代码在进入 synchronized 代码块前会自动获取内部锁，这时候其他线程访问该同步代码块时会被阻塞挂起。拿到内部锁的线程会在正常退出同步代码块或者抛出异常后或者在同步块内调用了该内置锁资源的 wait 系列方法时释放该内置锁。
内置锁：
即排它锁，也就是当一个线程获取这个锁后，其他线程必须等待该线程释放锁后才能获取该锁
> 由于 Java 中的线程是与操作系统的原生线程一一对应的，所以当阻塞一个线程时，需要从用户态切换到内核态执行阻塞操作，这是很耗时的操作，而 synchronized 的使用就会导致上下文切换。
## 关键字的作用
在并发编程中存在线程安全问题，使用 synchronized 关键字能够有效的避免多线程环境下的线程安全问题，线程安全问题主要考虑以下三点：
- 存在共享数据，共享数据是对多线程可见的，所有的线程都有权限对共享数据进行操作；
- 多线程共同操作共享数据。关键字 synchronized 可以保证在同一时刻，只有一个线程可以执行某个同步方法或者同步代码块，同时 synchronized 关键字可以保证一个线程变化的可见性
- 多线程共同操作共享数据且涉及增删改操作。如果只是查询操作，是不需要使用 synchronized 关键字的，在涉及到增删改操作时，为了保证数据的准确性，可以选择使用 synchronized 关键字。
## 三种使用方式
Java 中每一个对象都可以作为锁，这是 synchronized 实现同步的基础。synchronized 的三种使用方式如下：
- 普通同步方法（实例方法）：
  锁是当前实例对象 ，进入同步代码前要获得当前实例的锁；
- 静态同步方法：
  锁是当前类的 class 对象 ，进入同步代码前要获得当前类对象的锁；
- 同步方法块：
  锁是括号里面的对象，对给定对象加锁，进入同步代码库前要获得给定对象的锁。
按照锁的对象区分可以分为
按照在代码中的位置区分可以分为
## 锁的对象
### 对象锁
锁对象为当前
```java
public void synchronized method() {
    System.out.println("我是普通方法形式的对象锁");
}

public void method() {
    synchronized(this) {
        System.out.println("我是代码块形式的对象锁");
    }
}
```
### 类锁
锁的是当前类或者指定类的Class对象。一个类可能有多个实例对象，但它只可能有一个Class对象。
```java
public static void synchronized method() {
    System.out.println("我是静态方法形式的类锁");
}

public void method() {
    synchronized(*.class) {
        System.out.println("我是代码块形式的类锁");
    }
}
```
---
```java
public class MySynch implements Runnable{

    public void common() {
        System.out.println("线程 " + Thread.currentThread().getName() + " 正在执行");
        try {
            Thread.sleep(1000);
        } catch(InterruptedException e) {
            e.printStackTrace();
        }
        System.out.println("线程 " + Thread.currentThread().getName() + " 执行完毕");
    }

    public static void commonStatic() {
        System.out.println("线程 " + Thread.currentThread().getName() + " 正在执行");
        try {
            Thread.sleep(1000);
        } catch(InterruptedException e) {
            e.printStackTrace();
        }
        System.out.println("线程 " + Thread.currentThread().getName() + " 执行完毕");
    }

    @Override
    public void run() {
//        method1();
        method2();
//        method3();
        method4();
    }
    public synchronized void method1() {
        common();
    }
    public void method3() {
        synchronized(this) {
            common();
        }
    }
    public static synchronized void method2() {
        commonStatic();
    }
    public void method4() {
        synchronized(MySynch.class) {
            common();
        }
    }

    public static void main(String[] args) throws InterruptedException {
        Thread thread1 = new Thread(new MySynch(), "thread1");
        thread1.start();

        Thread thread2 = new Thread(new MySynch(), "thread2");
        thread2.start();

        thread1.join();
        thread2.join();
    }

}
```
- method1/method3
- method2/method4
## 异常释放锁
```java
public class MySynch2 implements Runnable{

    public void common() {
        System.out.println("线程 " + Thread.currentThread().getName() + " 正在执行");
        try {
            Thread.sleep(1000);
        } catch(InterruptedException e) {
            e.printStackTrace();
        }
        System.out.println("线程 " + Thread.currentThread().getName() + " 执行完毕");
    }

    @Override
    public void run() {
        if (Thread.currentThread().getName().equalsIgnoreCase("thread1")){
            method1();
        }else {
            method2();
        }

    }
    public synchronized void method1() {
        throw new RuntimeException("自定义异常.");
    }
    public void method2() {
        synchronized(this) {
            common();
        }
    }

    public static void main(String[] args) throws InterruptedException {
        MySynch2 mySynch2 = new MySynch2();
        Thread thread1 = new Thread(mySynch2, "thread1");
        Thread thread2 = new Thread(mySynch2, "thread2");
        thread1.start();

        thread2.start();
        thread1.join();
        thread2.join();
    }

}
```
运行上方代码的时候会发现：
- thread1
- 当
## 可重入性
```java
public class MySync3 implements Runnable {
    int a = 0;

    @Override
    public void run() {
        func1();
    }

    private synchronized void func1() {
        System.out.println("进入：" + a);
        if (a < 3) {
            a++;
            func1();
        }
        System.out.println("结束：" + a);
    }

    public static void main(String[] args) {
        MySync3 mySync3 = new MySync3();
        new Thread(new MySync3()).start();
    }
}
```
```bash
进入：0
进入：1
进入：2
进入：3
结束：3
结束：3
结束：3
结束：3
```
func1
这就是“可重入性”
## 实现原理
https://juejin.cn/post/6844903757709312014#heading-6
## JDK对synchronized的优化
jdk1.6之前synchronized是很重的，所以并不被开发者偏爱，随着后续版本jdk对synchronized的优化使其越来越轻量，它还是很好用的，甚至ConcurrentHashMap在jdk的put方法都在jdk1.8时从ReetrantLock.tryLock()改为用synchronized来实现同步。 并且还引入了偏向锁，轻量级锁等概念，下面是偏向锁和轻量级锁的获取流程
www.processon.com/diagraming/…
参考 《咕泡学院公开课》
# wait 与 notify
https://www.cnblogs.com/paddix/p/5381958.html
wait方法的使用必须在同步的范围内，否则就会抛出IllegalMonitorStateException异常，wait方法的作用就是阻塞当前线程等待notify/notifyAll方法的唤醒，或等待超时后自动唤醒。
既然wait方式是通过对象的monitor对象来实现的，所以只要在同一对象上去调用notify/notifyAll方法，就可以唤醒对应对象monitor上等待的线程了。notify和notifyAll的区别在于前者只能唤醒monitor上的一个线程，对其他线程没有影响，而notifyAll则唤醒所有的线程
经典的“生产者-消费者”模型就可基于此实现。
### wait 什么必须释放锁
当线程A进入synchronized(obj1)中之后，也就是对obj1上了锁。此时，调用wait()进入阻塞状态， 一直不能退出synchronized代码块;那么，线程B永远无法进入synchronized(obj1)同步块里，永远没 有机会调用notify()，发生死锁。
这就涉及一个关键的问题:在wait()的内部，会先释放锁obj1，然后进入阻塞状态，之后，它被另外 一个线程用notify()唤醒，重新获取锁!其次，wait()调用完成后，执行后面的业务逻辑代码，然后退出 synchronized同步块，再次释放锁。
```java
wait() {
  // 释放锁
  // 阻塞，等待被其他线程notify
  // 重新获取锁
}
```
### 问题
生产者在通知消费者的同时，也通知了其他的生产者;消费者在通知生产者的同时，也通知了其他 消费者。原因在于wait()和notify()所作用的对象和synchronized所作用的对象是同一个，只能有一个对 象，无法区分队列空和列队满两个条件。这正是Condition要解决的问题。
# interruptedException 与interrupt
个人理解：中断线程的当前状态。将中断标记修改。
https://www.cnblogs.com/skywang12345/p/3479949.html
### 什么情况下会抛出Interrupted异常
只有那些声明了会抛出InterruptedException的函数才会抛出异常，也就是下面这些常用的函数:
```java
public static native void sleep(long millis) throws InterruptedException
{...}
public final void wait() throws InterruptedException {...}
public final void join() throws InterruptedException {...}
```
### 轻量级阻塞和重量级阻塞
能够被中断的阻塞称为轻量级阻塞，对应的线程状态是WAITING或者TIMED_WAITING;而像 synchronized 这种不能被中断的阻塞称为重量级阻塞，对应的状态是 BLOCKED。
# 优雅关闭线程
https://www.cnblogs.com/skywang12345/p/3479949.html

## 最佳实践

- **优先 Runnable/Callable 而非继承 Thread**：组合优于继承，且 Callable 可返回结果
- **synchronized 粒度要小**：同步代码块优于同步方法，减少锁持有时间
- **wait/notify 必须在同步块内**：配合 while 条件循环使用（防虚假唤醒）
- **中断是协作机制**：interrupt 只是发信号，线程自行决定何时响应
- **优雅关闭**：用标志位 + interrupt 配合，不要用 stop()（已废弃，不安全）

## 常见踩坑

| 编号 | 坑 | 现象 | 解决方案 |
|---|---|---|---|
| #T1 | wait/notify 不在同步块 | IllegalMonitorStateException | 必须在 synchronized 块内调用 |
| #T2 | 用 if 判断等待条件 | 虚假唤醒导致逻辑错误 | 用 while 循环检查条件 |
| #T3 | 用 stop() 停线程 | 数据不一致/资源不释放 | interrupt + 标志位 |
| #T4 | 类锁对象锁混淆 | 同步失效 | 静态方法锁类，实例方法锁对象 |
| #T5 | 死锁（互相等待） | 程序卡死 | 锁顺序一致/超时锁（见 [01-JUC之锁与AQS](JUC/01-JUC之锁与AQS.md)） |
| #T6 | notify 误用 notifyAll | 部分线程永远等不到 | 不确定唤醒哪个时用 notifyAll |

## 小结

- 并发三要素：原子性/有序性/可见性；线程五状态：创建/就绪/运行/阻塞/死亡
- 并发问题：数据竞争/死锁/活锁/资源不足/优先权反转
- 三种创建线程方式：Thread/Runnable/Callable
- synchronized：代码块/方法/静态方法，对象锁 vs 类锁，可重入
- wait/notify 协作 + interrupt 中断 + 标志位优雅关闭

## 下一篇

[JMM内存模型详解](../../JVM/JMM内存模型详解.md)——可见性/有序性的底层原理

## 参考资料

- [Oracle Java Tutorials: Concurrency](https://docs.oracle.com/javase/tutorial/essential/concurrency/)，查询日期：2026-08-09
