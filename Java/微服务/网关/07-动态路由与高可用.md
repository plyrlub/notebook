---
tags: [Java, 微服务, Spring Cloud Gateway, 动态路由, 高可用, Nacos, WebSocket, 学习笔记]
创建日期: 2026-08-20
状态: ✅ 已归档（01-学习/Java/微服务/网关）
归属: 01-学习/Java/微服务/网关
上一篇: 06-Actuator运维实操
下一篇: 08-网关鉴权详解
---

# 动态路由与高可用

> 承接 [01-Spring Cloud Gateway详解](01-Spring Cloud Gateway详解.md)。本文把「WebSocket 路由、动态路由、Nacos 配置实战、生产实践」从详解总篇拆出独立成章，关乎网关的可扩展与生产落地：
> - §1 WebSocket 路由
> - §2 动态路由与配置中心刷新（Nacos）
> - §3 配置实战：Nacos 服务发现 + 路由
> - §4 生产最佳实践
> 关联：Actuator 运行时调路由见 [06-Actuator运维实操](06-Actuator运维实操.md)；网关鉴权见 [08-网关鉴权详解](08-网关鉴权详解.md)。

---
## 1. WebSocket 路由

WebSocket 请求由 `WebsocketRoutingFilter` 处理，根据 `exchange` 中 `GATEWAY_REQUEST_URL_ATTR` 的 scheme 是否为 `ws://`/`wss://` 决定是否代理。

```yaml
spring:
  cloud:
    gateway:
      routes:
        - id: websocket_route
          uri: ws://localhost:8080          # ws/wss 前缀
          predicates: [ Path=/ws/** ]
        - id: websocket_lb
          uri: lb:ws://chat-service        # lb 前缀 + ws，经服务发现负载均衡
          predicates: [ Path=/ws/** ]
```
- 需负载均衡用 `lb:ws://` 前缀。
- 若使用 **SockJS**（普通 HTTP 作后备），须同时配置 HTTP 路由与 WebSocket 路由。

---


## 2. 动态路由与配置中心刷新（Nacos）

### 2.1 定位器（RouteDefinitionLocator）

路由来源由多个 `RouteDefinitionLocator` 提供：

| 定位器 | 来源 |
|---|---|
| `PropertiesRouteDefinitionLocator` | 配置文件（yaml/properties）静态路由 |
| `DiscoveryClientRouteDefinitionLocator` | 注册中心（Nacos/Eureka）自动发现路由 |
| `RouteDefinitionRepository` | 内存 / 配置中心 / MySQL / Redis 等动态源 |

### 2.2 动态路由实现（Nacos）

配置中心驱动动态路由有两种常用方式：

**方式一：Nacos 托管路由配置（yaml 配在 Nacos，`RefreshRoutesEvent` 刷新）**

路由配置存入 Nacos 数据中心，网关引入 Nacos Config 依赖并在 `bootstrap.yml` 声明配置源。配置变更自动触发 Context Refresh 后，监听刷新事件重载路由：

```java
@Component
public class GatewayRoutesRefresher implements ApplicationListener<RefreshScopeRefreshedEvent> {
    @Autowired
    private RouteDefinitionWriter routeDefinitionWriter;
    @Autowired
    private ApplicationEventPublisher publisher;

    @EventListener(RefreshRoutesEvent.class)
    public void onRefresh() {
        // 重新从 RouteDefinitionRepository 加载路由并发布刷新事件
        publisher.publishEvent(new RefreshRoutesEvent(this));
    }
}
```

**方式二：自定义 `RouteDefinitionRepository`**（从 Nacos JSON / MySQL 读取并监听变更）

实现 `RouteDefinitionRepository`（继承 `RouteDefinitionLocator` + `RouteDefinitionWriter`），利用 Nacos Config `Listener` 监听配置变更，变更后发布 `RefreshRoutesEvent`。

```java
@Component
public class NacosRouteDefinitionRepository implements RouteDefinitionRepository {
    // getRouteDefinitions(): 从数据源取路由列表
    // save(route) / delete(routeId): 增删路由
    // 监听 config 变化 → RouteDefinitionWriter 更新 → 发布 RefreshRoutesEvent
}
```

```java
// 手动触发全量刷新
applicationEventPublisher.publishEvent(new RefreshRoutesEvent(this));
```

> **注意**：动态路由频繁变更会触发路由表重建，高频率刷新可能带来 CPU 占用；生产按变更频率合理节流 / 预热。

### 2.3 动态路由的两条路径：Actuator 运行时 vs 配置中心 / Repository

Spring Cloud Gateway 支持「运行时动态增删路由」的两条途径，二者互补、适用的场景不同：

| 维度 | Actuator 端点直接增删（[06-Actuator运维实操](06-Actuator运维实操.md) §1.2.6） | 配置中心 / 自定义 Repository（本节） |
|---|---|---|
| 手段 | `POST/DELETE /actuator/gateway/routes/{id}` + `/refresh` | Nacos 托管配置 / `RouteDefinitionRepository` + `RefreshRoutesEvent` |
| 触发方式 | 开发者 / 运维手动调接口 | 配置中心变更自动监听、代码驱动 |
| 权限 | 需 `management.endpoint.gateway.access=unrestricted` + 鉴权保护 | 在应用代码内，受控性强 |
| 适合场景 | 临时调试、应急改路由、快速试放 | 生产常态化动态路由、对接配置中心/数据库 |
| 持久化 | 端点创建的路由存内存，重启丢失 | 存配置中心/数据库，重启可恢复 |
| 与配置路由关系 | 只能改/删**端点创建**的路由；配置/`@Bean` 路由只读（删返回 404） | 可完全接管路由来源 |

**实践建议**：
- **生产常态化动态路由** → 用 §2.2 的配置中心 / Repository（可持久化、可恢复、受控）。
- **临时调试 / 应急 / 首发灰度放量** → 用 [06-Actuator运维实操](06-Actuator运维实操.md) §1.2.6 的 Actuator 端点即时增删，配合 `/refresh` 立即生效。
- **组合**：日常以配置中心 / Repository 为路由单一事实来源；仅在需要临时应急改路由或手工验证时用 Actuator 端点补充，改完记得走持久化通道落回配置源。
- 无论哪条路径，改完都要触发路由刷新才能生效：配置中心走 `RefreshRoutesEvent`，Actuator 走 `/actuator/gateway/refresh`（两端点本质都在重建路由缓存）。

### 2.4 场景细化：多 route 配置 / 整体配置放配置中心

#### 2.4.1 配置多个 route（多 service 路由表）

日常常见的是给多个微服务各配一条 route，全部写在同一份 `spring.cloud.gateway.routes` 下；前缀划分即可，不必每个服务一套 spring 上下文：

```yaml
spring:
  cloud:
    gateway:
      discovery:
        locator:
          enabled: false          # 用显式 route，不用自动发现
      routes:
        - id: user-service
          uri: lb://user-service
          predicates: [ Path=/api/user/** ]
          filters: [ StripPrefix=2 ]
        - id: order-service
          uri: lb://order-service
          predicates: [ Path=/api/order/** ]
          filters: [ StripPrefix=2 ]
        - id: payment-service
          uri: lb://payment-service
          predicates: [ Path=/api/payment/** ]
          filters: [ StripPrefix=2 ]
```

> 要点：多 route 本质是 `routes` 列表的多个元素；匹配仍按顺序优先（先命中先走），精确前缀在前、宽通配在后。若希望「某 route 全局 default 化」而非逐条重复，可配合 `default-filters` 抽公共逻辑（如统一加头、统一 `StripPrefix`）。

#### 2.4.2 整体网关配置放配置中心（不止路由）

动态路由不只有「把 route 列表放 Nacos」一种姿势，整套 `spring.cloud.gateway` 配置都可以下放到配置中心，改配置即热更新，无需重启网关：

```yaml
# Nacos 中 gateway 的配置内容（dataId=gateway-service.yml, 可带 group）
spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes: [ ]                      # 路由可全量放这里
      default-filters: [ ]             # 全局默认过滤器
      globalcors:
        cors-configurations: { }       # 跨域
      httpclient:
        connect-timeout: 4000
      discovery:
        locator:
          enabled: false
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
```

实现分两种：

| 方式 | 做法 | 是否需重启 | 适用 |
|---|---|---|---|
| A. `RouteDefinitionRepository` 独立接管路由 | 只把 route 交给自定义 Repository + `RefreshRoutesEvent`（见 §2.2 方式二） | 否 | 只热更新路由 |
| B. 配置中心驱动整个 Gateway 配置 | 引入 `spring-cloud-starter-alibaba-nacos-config` + `bootstrap`/`spring.config.import` 声明配置源；`spring.cloud.gateway.*` 随配置中心刷新被 `@ConfigurationProperties` 重新绑定 | 部分 | 路由 + 过滤器 + CORS + 超时整套动态 |

> 实践结论：只需热更新路由 → 用 A（监听 Nacos + `RouteDefinitionRepository` + 发 `RefreshRoutesEvent`）；要整套网关配置（含路由/跨域/超时）都能改 → 用 B（nacos-config 接管 `spring.cloud.gateway.*`，改动由配置中心刷新 + 属性重绑定生效）。A 更精准、B 更全面。

#### 2.4.3 生产注意：刷新性能与「零宕机」

动态路由热更新在生产有三点常见坑：

1. **高频刷新 → CPU 飙升**：每次 `RefreshRoutesEvent` 都会重建路由表。配置频繁变更需**节流/合并**（延迟批量、版本号变更才触发），避免每几秒全量重建。
2. **刷新空窗 → 短暂 502**：`RouteDefinitionRouteLocator` 收到新配置默认「先清空旧路由表，再逐条解析新路由」，清空到解析完之间路由表是空的，期间请求全部 404/502。路由多/配置复杂时窗口可能到数百 ms。可**采用双缓冲**：新路由解析校验完再一次性切换，避免「先清后装」的空窗。
3. **失败不要覆盖**：若新路由解析/校验出错，应保留旧路由表而不是用坏配置覆盖，确保热更新「失败可回退」。

> 配套：路由版本管理 + 一键回滚（配置中心版本历史即可实现）；灰度发布（配合 `Weight` 谓词或 Nacos 灰度发布渐进放量）。这块与 [04-内置过滤器详解](04-内置过滤器详解.md) 的 Weight 断言、生产 §4 高可用一起看更完整。

---


## 3. 配置实战：Nacos 服务发现 + 路由

依赖：
```xml
<dependency>org.springframework.cloud:spring-cloud-starter-gateway</dependency>
<dependency>com.alibaba.cloud:spring-cloud-starter-alibaba-nacos-discovery</dependency>
```
配置：
```yaml
server:
  port: 8080
spring:
  application:
    name: gateway
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    gateway:
      discovery:
        locator:
          enabled: true        # 为已注册服务自动生成路由（可选）
      routes:
        - id: user-service
          uri: lb://user-service
          predicates: [ Path=/api/user/** ]
          filters: [ StripPrefix=2 ]
        - id: order-service
          uri: lb://order-service
          predicates: [ Path=/api/order/** ]
          filters: [ StripPrefix=2 ]
```
链路：
```
Gateway 启动 → 注册到 Nacos → 客户端请求 http://网关:8080/api/user/xx
→ 路由匹配 → lb://user-service 负载均衡 → 转发到实例
```
> **要点**：`lb://服务名` 走服务发现负载均衡；`discovery.locator.enabled=true` 自动把注册服务映射为路由（路径默认 `/服务名/**`），适合非敏感场景，注意与显式路由的前缀冲突。

---


## 4. 生产最佳实践

### 4.1 原则清单

- **统一鉴权放网关**：Reactive Security/JWT 一次配置，下游免重复认证；透传身份头。
- **网关限流挡入口**：RequestRateLimiter（Redis）按用户 / IP / 接口，超限 429；服务内 Sentinel 兜底。
- **CORS 网关统一配**：避免各服务各配不同策略。
- **重试 / 熔断**：Retry（可重试状态）+ CircuitBreaker（fallback）提升可用性。
- **白名单**：`/auth`、`/actuator` 等 permitAll，其余 authenticated。
- **路由简洁**：避免前缀重复，用 `order` 显式排优先级；精确路径在前、通配在后。
- **监控**：配 Actuator/Metric，观察限流 429 数、熔断事件、路由命中。

### 4.2 网关 vs Nginx 边界

```
客户端 → [Nginx(服务端入口/LB) → Spring Cloud Gateway(应用网关) → 微服务]
            ↑7层LB/静态/SSL            ↑业务路由/鉴权/限流/转发
```

| 维度 | Nginx | Spring Cloud Gateway |
|---|---|---|
| 定位 | 服务端入口 / 反向代理 | 应用 / 业务网关 |
| 主要能力 | 静态资源、7 层负载均衡、SSL、反向代理 | 业务路由、统一鉴权、限流、重试、熔断 |
| 关注点 | 网络 / 流量分发 | 业务 / 微服务协作 |
| 运维 | 运维主导 | 开发主导 |
| QoS 控制 | 基本 | 强（令牌桶 / Resilience4j / Sentinel） |
| 动态路由 / 服务发现 | 配置为主 | 支持（Nacos/CI） |

**结论**：Nginx 管"流量进不进、分给谁"（LB/静态/SSL），Gateway 管"业务请求怎么处理、鉴权限流、转发到哪个服务"。大流量通常 Nginx 前置 + Gateway 后置双网关；简单场景 Nginx 可直接到服务。

### 4.3 高可用 / 横向扩展

- **网关无状态** → 可**多实例横向扩展** + 前置负载均衡（Nginx / 云 LB），提升吞吐与可用。
- **多实例**：实例注册 Nacos，LB 分发；无本地 session（限流 / 熔断状态存 Redis）。
- **依赖外部状态**：限流状态（Redis）、路由/规则配置（Nacos）、熔断状态（Redis 可选）；**无状态化**才能水平扩。
- **健康检查**：接 Actuator `/actuator/health`，LB 自动摘除故障实例；下线先摘流量再停。

```
Client → LB(Nginx/SLB) → Gateway实例1/2/3(注册Nacos, 共享Redis) → 微服务
```

### 4.4 背压（Backpressure）

背压 = 协调上下游速度不匹配——消费者处理慢时反馈生产者"慢点发"，防内存溢出 / 崩溃，是响应式核心机制。

```
生产者(快) ──request(n)──> 消费者(慢)
          <── 背压信号 ──
request(n): 消费者按自身处理能力请求数据量 → 生产者只发 n 个
```

| 机制 | 维度 | 区别 |
|---|---|---|
| 背压 | 流式数据量订阅 | 响应式 `request(n)`，消费者主动控制拉取速率 |
| 限流 | 请求速率 | 限 QPS，超限拒 / 队列 |
| 熔断 | 下游故障 | 持续故障切断 |

> 背压是消费者保护自己（慢点接）；限流是生产者保护自己（少放行）。Gateway 基于 WebFlux，接收 / 转发天然具备背压协调能力。

```java
Flux<Item> stream = source.onBackpressureDrop();      // 超出能力直接丢弃(配套告警)
// 或 onBackpressureBuffer(capacity) / onBackpressureError
```

### 4.5 优雅停机 / 服务下线

**为何不能裸 kill**：`kill -9` 强杀 → ①在途任务中断 ②未从注册中心下线，consumer / 网关仍调用 → `Connection refused`。

**优雅下线三步**（发布 / 扩容时用户无感知）：
```
① 摘流量：先从 LB/注册中心把实例摘除或标记下线，不再收新请求
② 处理在途：kill -15 / 触发优雅停机，处理完 in-flight 请求再退出
③ 关闭资源：释放连接池/线程池/后台任务，再真正退出
```

**Boot 优雅停机配置**：
```yaml
server:
  shutdown: graceful                  # Boot 2.3+ 支持
spring:
  lifecycle:
    timeout-per-shutdown-phase: 30s   # 最多等 30s 处理在途请求
```
**配合注册中心 / K8s**：发布前先 `POST /actuator/shutdown`（或 kill -15）+ 注册中心 `deregister`；K8s 配 `preStop` hook + 就绪探针。

---
