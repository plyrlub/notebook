---
tags: [CI/CD, 学习笔记]
创建日期: 2026-08-10
状态: ✅ 已归档（01-学习/DevOps/CI-CD）
归属: 01-学习/DevOps/CI-CD
---

# 第十章：进阶与工程化

> 学习目标：从"能跑通 CI/CD"升级到"**规模化、可复用、可度量**的工程化体系"。覆盖流水线复用（template/include）、Monorepo 多服务 CI/CD 策略、IaC 基础设施即代码、DORA 指标度量与持续改进，以及你的 **P1.3 实践全链路回顾与演进路线**。
>
> 本章是全书最后一章，定位为"从中级到高级的跃迁指南"——前面 01~09 章解决了"每个环节怎么做"，10 章解决"**怎么规模化、怎么度量、怎么持续改进**"。
>
> 关联补充专题：[S2 成本与配额](./补充专题/S2-成本与配额.md)、[S3 DORA 四指标](./补充专题/S3-DORA四指标.md)

---

## 目录结构（编号一览）

| 一级 | 二级 | 三级 | 内容 | 输出形式 |
| ---- | ---- | ---- | ---- | -------- |
| **10.1** | | | 流水线复用与模板化 | — |
| | 10.1.1 | | 为什么要复用（DRY 原则在 CI/CD 中的应用） | 反模式对比 |
| | 10.1.2 | | GitLab CI include + template + extends 三种复用机制 | .gitlab-ci.yml |
| | 10.1.3 | | 流水线模板库设计（ci-templates 仓 + 版本化引用） | 仓库结构 |
| **10.2** | | | Monorepo CI/CD 策略 | — |
| | 10.2.1 | | Monorepo vs Polyrepo 权衡（什么时候该上 Monorepo） | 对比表 |
| | 10.2.2 | | Monorepo 流水线核心：变更检测 + 增量构建 + 依赖图 | .gitlab-ci.yml |
| | 10.2.3 | | Monorepo 下的 P1.3 四段环境适配 | — |
| **10.3** | | | IaC 基础设施即代码 | — |
| | 10.3.1 | | IaC 是什么（Terraform / Ansible / Pulumi 定位与选型） | 对比表 |
| | 10.3.2 | | Terraform 基础：HCL 语法 + state 管理 + 模块化 | .tf 示例 |
| | 10.3.3 | | IaC + CI/CD：基础设施变更也走 Pipeline | GitLab CI |
| **10.4** | | | DORA 指标度量与持续改进 | — |
| | 10.4.1 | | DORA 四指标回顾与采集落地（衔接 S3 + 09 章） | 采集架构图 |
| | 10.4.2 | | 从指标到行动：Low → Medium → High → Elite 晋升路径 | 改进路线图 |
| | 10.4.3 | | CI/CD 成熟度自评清单（20 项打分表） | 评分表 |
| **10.5** | | | P1.3 全链路回顾与演进路线 | — |
| | 10.5.1 | | 01~09 章知识点 × P1.3 实践对照全表 | 全景表 |
| | 10.5.2 | | 小团队 6 个月演进路线图（从起步到 Elite） | 里程碑图 |
| **10.6** | | | 生产常见问题与事故复盘 | 事故+解 |
| **10.7** | | | 全书小结与自检（15 个必答问题） | 问题清单 |

---

## 10.1 流水线复用与模板化

### 10.1.1 为什么要复用（DRY 原则在 CI/CD 中的应用）

**反模式**：每个项目仓库里都有一份 `.gitlab-ci.yml`，10 个服务 = 10 份 300 行的 YAML，90% 内容相同（都是 lint → test → build → deploy）。某天要给所有服务加一个安全扫描 Job，需要改 10 个仓库的 10 份 YAML——漏改一个就是安全漏洞。

**复用后的效果**：10 个服务的 `.gitlab-ci.yml` 各只有 10 行（引用公共模板），公共逻辑集中在 1 个 `ci-templates` 仓里。改 1 次，10 个服务全部生效。

**表 10-1：复用前 vs 复用后**

| 维度 | 复用前（每仓一份） | 复用后（模板 + include） |
| ---- | ------------------ | ------------------------ |
| 新服务接入 CI/CD | 复制粘贴 300 行 YAML，改 1 小时 | 写 10 行 include，5 分钟 |
| 全局变更（如加安全扫描） | 改 N 个仓库，漏改风险高 | 改 1 个模板仓，全部生效 |
| 流水线规范一致性 | 每个仓库自己一套，逐渐分化 | 统一模板，强制规范 |
| 学习成本 | 新人要看每个仓的 CI 才知道怎么跑 | 看模板仓一次，所有仓都懂 |

### 10.1.2 GitLab CI include + template + extends 三种复用机制

**机制一：include（引用外部 YAML 文件）**

```yaml
# myapp/.gitlab-ci.yml（业务仓库，只有 10 行）
include:
  # 引用 ci-templates 仓的 Python 模板
  - project: 'devops/ci-templates'
    ref: 'v2.1.0'           # 锁定版本，不要用 main（防止模板改了炸所有项目）
    file: '/python/fastapi-base.yml'

  # 引用安全扫描模板
  - project: 'devops/ci-templates'
    ref: 'v2.1.0'
    file: '/security/sast-scan.yml'

# 只写本项目特有的配置
variables:
  APP_NAME: myapp
  SERVICE_PORT: "8000"

# 覆盖模板里的默认值（如改 deploy 目标）
deploy:prod:
  variables:
    DEPLOY_HOST: prod-myapp-01.internal
```

**机制二：extends（继承 + 覆盖单个 Job）**

```yaml
# ci-templates/python/fastapi-base.yml（模板仓）
# 定义可复用的 Job 模板（用 . 开头表示隐藏 Job，不直接执行）
.test-template:
  stage: test
  image: python:3.12-slim
  before_script:
    - pip install -e ".[dev]" --quiet
  script:
    - pytest --cov=app --cov-report=xml --cov-report=term
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml

# 业务仓继承 + 覆盖
test:unit:
  extends: .test-template          # 继承模板
  variables:
    COVERAGE_THRESHOLD: "80"       # 覆盖/追加变量
  script:
    - pytest tests/unit/ --cov=app --cov-report=xml    # 覆盖 script
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
```

**机制三：trigger（跨仓流水线触发）**

```yaml
# myapp/.gitlab-ci.yml
# 镜像构建完成后，触发部署仓的流水线（解耦构建和部署）
trigger:deploy:
  stage: deploy
  needs: ["build:kaniko"]
  trigger:
    project: 'devops/deploy-pipeline'   # 触发另一个项目的 CI
    branch: main
    strategy: depend                     # 等待下游 CI 完成才算本 Job 成功
  variables:
    IMAGE_TAG: $CI_COMMIT_TAG            # 传递变量给下游
    TARGET_ENV: prod
```

**三种机制对比**：

| 机制 | 用途 | 粒度 | 适合场景 |
| ---- | ---- | ---- | -------- |
| **include** | 引入整份 YAML 文件 | 文件级 | 公共流水线模板（所有 Python 服务共享） |
| **extends** | 继承 + 覆盖单个 Job | Job 级 | 微调模板里的某个 Job（改 image / 改 script） |
| **trigger** | 触发另一个项目的 CI | 项目级 | 构建仓和部署仓分离、Monorepo 触发子项目 |

### 10.1.3 流水线模板库设计（ci-templates 仓 + 版本化引用）

**ci-templates 仓库结构**：

```
ci-templates/
├── README.md
├── python/
│   ├── fastapi-base.yml        # FastAPI 基础模板（lint + test + build）
│   ├── django-base.yml         # Django 基础模板
│   └── library-base.yml        # Python 库模板（发布到 PyPI）
├── java/
│   ├── spring-boot-base.yml    # Spring Boot 基础模板
│   └── maven-library.yml       # Maven 库模板
├── security/
│   ├── sast-scan.yml           # SAST 扫描模板（→ 08 章）
│   ├── sca-scan.yml            # SCA 依赖扫描模板
│   └── secret-detection.yml    # 密钥扫描模板
├── deploy/
│   ├── k8s-deploy.yml          # K8s 部署模板（→ 07 章）
│   ├── docker-compose-deploy.yml  # Docker Compose 部署模板（→ 07 章 7.6.4）
│   └── swarm-deploy.yml        # Swarm 部署模板
├── monitoring/
│   ├── grafana-sync.yml        # Grafana 仪表盘同步（→ 09 章 9.9.2）
│   └── verify-post-deploy.yml  # 部署后 Verify（→ 09 章 9.9.2）
└── tags/                       # 版本标签
    ├── v1.0.0/                 # 旧版本（向后兼容）
    ├── v2.0.0/
    └── v2.1.0/                 # 当前版本
```

**版本化引用铁律**：
1. **永远用 `ref: 'vX.Y.Z'` 锁定版本**，不要用 `ref: 'main'`——模板改了会直接炸所有引用方
2. **模板变更走 MR + Code Review**，和业务代码同等对待
3. **Breaking Change 升大版本号**（v1→v2），给引用方时间迁移
4. **模板仓的 CHANGELOG.md 记录每次变更**，方便引用方判断要不要升级

> **小团队起步建议**：先用 GitLab CI 原生的 `include:template`（GitLab 自带模板），等服务 > 5 个再建自己的 `ci-templates` 仓。不要一上来就建模板仓——过早抽象比重复更痛苦。

---

## 10.2 Monorepo CI/CD 策略

### 10.2.1 Monorepo vs Polyrepo 权衡（什么时候该上 Monorepo）

**表 10-2：Monorepo vs Polyrepo 对比**

| 维度 | Polyrepo（多仓） | Monorepo（单仓） |
| ---- | ---------------- | ---------------- |
| 仓库结构 | 每个服务一个 Git 仓 | 所有服务在一个 Git 仓 |
| 跨服务修改 | 改 2 个仓 + 2 个 MR + 2 次 CI | 改 1 个仓 + 1 个 MR + 1 次 CI |
| 代码共享 | 发包到内部 PyPI/私有 npm | 直接 import 同仓代码 |
| 版本一致性 | 服务 A 依赖服务 B v1.2，B 已经 v1.3 了 | 所有服务同一次 commit 里版本一致 |
| CI/CD 复杂度 | ⭐ 简单（每仓独立流水线） | ⭐⭐⭐⭐ 复杂（需要变更检测 + 增量构建） |
| 仓库体积 | 小 | 大（需要 Git LFS / 部分克隆） |
| 权限隔离 | 天然隔离（每仓独立权限） | 需要工具（CODEOWNERS + 路径权限） |
| 适合规模 | < 5 个服务 | 5~50 个服务 |

**什么时候该上 Monorepo**：
1. 跨服务修改频繁（改一个接口要同时改 3 个服务）
2. 有大量共享代码（公共库 / DTO / proto 定义）
3. 团队 > 5 人但 < 30 人（超 30 人需要 Bazel 等专业工具）
4. 愿意投入 CI/CD 基础设施（变更检测 + 增量构建 + 依赖图）

**什么时候不该上**：
1. 只有 2~3 个独立服务，很少跨服务修改
2. 团队 < 5 人，没有精力维护 Monorepo CI 基础设施
3. 服务间技术栈完全不同（Python + Java + Go 混在一起会增加复杂度）

> **小团队建议**：< 5 个服务用 Polyrepo + ci-templates 仓（10.1 节方案）就够了。Monorepo 的 CI 复杂度远超收益。等跨服务修改痛到受不了再迁移。

### 10.2.2 Monorepo 流水线核心：变更检测 + 增量构建 + 依赖图

**Monorepo 最大的 CI 挑战**：一个仓里 10 个服务，改了服务 A，不应该触发服务 B 的 CI（否则每次 push 跑 10 个服务的流水线 = 巨大浪费）。

**变更检测**（只对变更的服务跑 CI）：

```yaml
# .gitlab-ci.yml（Monorepo 变更检测）
# 仓库结构：
# services/
# ├── user-service/
# ├── order-service/
# └── payment-service/
# shared/
# ├── proto/           # 共享 proto 定义
# └── utils/           # 共享工具库

# === 1. 变更检测 Job：找出哪些服务变了 ===
detect-changes:
  stage: .pre
  script:
    - |
      # 获取本次 push 变更的文件列表
      CHANGED_FILES=$(git diff --name-only HEAD~1 HEAD)
      CHANGED_SERVICES=""

      # 检测哪些服务有变更
      for service in services/*/; do
        service_name=$(basename "$service")
        if echo "$CHANGED_FILES" | grep -q "^services/$service_name/"; then
          CHANGED_SERVICES="$CHANGED_SERVICES $service_name"
        fi
      done

      # shared/ 变了 → 所有服务都要跑（因为都依赖 shared）
      if echo "$CHANGED_FILES" | grep -q "^shared/"; then
        echo "shared 目录变更，所有服务都需要跑 CI"
        CHANGED_SERVICES="user-service order-service payment-service"
      fi

      # 写入环境变量供下游 Job 使用
      echo "CHANGED_SERVICES=$CHANGED_SERVICES" >> .changed-services.env
  artifacts:
    reports:
      dotenv: .changed-services.env
  rules:
    - if: $CI_COMMIT_BRANCH

# === 2. 动态生成子流水线 ===
trigger-services:
  stage: build
  needs: ["detect-changes"]
  script:
    - |
      for service in $CHANGED_SERVICES; do
        echo "触发 $service 的子流水线"
      done
  trigger:
    include:
      - local: '/services/$SERVICE/.gitlab-ci.yml'   # 每个服务有自己的 CI 配置
    strategy: depend
  parallel:
    matrix:
      - SERVICE: $CHANGED_SERVICES
  rules:
    - if: $CI_COMMIT_BRANCH
```

**增量构建**（只构建变更的服务镜像）：

```yaml
# services/order-service/.gitlab-ci.yml（子流水线）
build:order-service:
  stage: build
  image:
    name: gcr.io/kaniko-project/executor:debug
    entrypoint: [""]
  script:
    - |
      /kaniko/executor \
        --context=$CI_PROJECT_DIR/services/order-service \
        --dockerfile=$CI_PROJECT_DIR/services/order-service/Dockerfile \
        --destination=$CI_REGISTRY_IMAGE/order-service:$CI_COMMIT_SHORT_SHA \
        --cache=true \
        --cache-repo=$CI_REGISTRY_IMAGE/order-service:cache
  rules:
    - if: $CI_COMMIT_BRANCH
```

**依赖图**（shared 变了，下游服务怎么自动发现）：

```
shared/proto/  →  services/user-service/
                →  services/order-service/
                →  services/payment-service/

shared/utils/  →  services/user-service/
                →  services/order-service/

# 如果 shared/proto/order.proto 变了 → 只重建 order-service + payment-service
# 如果 shared/utils/common.py 变了 → 重建所有用了 utils 的服务
```

> **Monorepo CI 的投入产出比拐点**：服务 < 5 个时，每次跑全量 CI 也就 10 分钟，变更检测的复杂度不值得。服务 > 10 个时，不做变更检测每次跑 30+ 分钟，必须做。5~10 个之间看团队承受能力。

### 10.2.3 Monorepo 下的 P1.3 四段环境适配

**Monorepo + P1.3 的挑战**：feature 分支包含多个服务的变更，merge 到 dev/test/stage/main 时，每个服务都要独立部署到对应环境。

**适配方案**：

```
feature/order-v2（同时改了 order-service + payment-service）
    │
    ├─ merge dev
    │   ├─ deploy order-service → dev 环境
    │   └─ deploy payment-service → dev 环境
    │
    ├─ merge test
    │   ├─ deploy order-service → test 环境
    │   └─ deploy payment-service → test 环境
    │
    └─ merge main（批次到了）
        ├─ deploy order-service → main 验证（不发布）
        └─ deploy payment-service → main 验证（不发布）
```

**关键规则**：
1. **一个 feature 可以改多个服务**，但 merge 时所有变更的服务一起部署到目标环境
2. **回滚时一起回滚**（feature 里改的多个服务是一个原子单元，不能只回滚其中一个）
3. **批次发布时所有服务一起打 Tag**（order-service v1.2.0 + payment-service v1.2.0 同批次发布）

---

## 10.3 IaC 基础设施即代码

### 10.3.1 IaC 是什么（Terraform / Ansible / Pulumi 定位与选型）

**IaC 的核心思想**：基础设施（服务器、网络、数据库、负载均衡）用**代码**描述，而不是手动在云控制台点击。代码存 Git 仓库，走 CI/CD，可 Review、可回滚、可审计。

**手动 vs IaC 的差距**：
- 手动：创建一台 VM → 登录云控制台 → 点 20 下 → 15 分钟 → 没人记得配了什么
- IaC：`terraform apply` → 30 秒 → Git 里有完整配置历史

**表 10-3：IaC 工具选型对比**

| 工具 | 类型 | 语言 | 优势 | 劣势 | 适合场景 |
| ---- | ---- | ---- | ---- | ---- | -------- |
| **Terraform** | 声明式 | HCL | 生态最大、Provider 最全、state 管理 | HCL 学习曲线、state 文件管理 | **云资源管理（首选）** |
| **Pulumi** | 声明式 | Python/TS/Go | 用熟悉的编程语言写 IaC | 生态小、企业版收费 | 开发团队偏好编程语言 |
| **Ansible** | 过程式 | YAML | 简单、不需要 Agent、配置管理强 | 不擅长云资源创建 | **服务器配置管理（首选）** |
| **Crossplane** | 声明式 | YAML（K8s CRD） | 用 K8s 管理 everything | 学习成本高、成熟度低 | K8s 原生团队 |

**小团队推荐组合**：Terraform（管云资源：VM/VPC/RDS）+ Ansible（管服务器配置：装 Nginx/Python/配置文件）。两个都是免费开源，覆盖 90% 场景。

### 10.3.2 Terraform 基础：HCL 语法 + state 管理 + 模块化

**Terraform HCL 示例**（创建一台 VM + 安全组）：

```hcl
# main.tf
# === Provider 配置（阿里云/AWS/腾讯云，这里以阿里云为例）===
provider "alicloud" {
  region = "cn-hangzhou"
}

# === 安全组 ===
resource "alicloud_security_group" "myapp_sg" {
  name        = "myapp-sg"
  description = "Security group for myapp"
}

resource "alicloud_security_group_rule" "allow_http" {
  type              = "ingress"
  ip_protocol       = "tcp"
  port_range        = "8000/8000"
  security_group_id = alicloud_security_group.myapp_sg.id
  cidr_ip           = "0.0.0.0/0"
}

resource "alicloud_security_group_rule" "allow_ssh" {
  type              = "ingress"
  ip_protocol       = "tcp"
  port_range        = "22/22"
  security_group_id = alicloud_security_group.myapp_sg.id
  cidr_ip           = "10.0.0.0/8"    # 只允许内网 SSH
}

# === ECS 实例 ===
resource "alicloud_instance" "myapp_server" {
  instance_type   = "ecs.g6.large"     # 2C 8G
  image_id        = "ubuntu_22_04_x64"
  security_groups = [alicloud_security_group.myapp_sg.id]
  instance_name   = "myapp-prod-01"

  tags = {
    Env     = "prod"
    Service = "myapp"
    Managed = "terraform"     # 标记：Terraform 管理的资源
  }
}

# === 输出 ===
output "server_ip" {
  value = alicloud_instance.myapp_server.public_ip
}
```

**state 管理**（Terraform 最关键的概念）：

```hcl
# backend.tf（state 文件存远程，不要存本地）
terraform {
  backend "oss" {              # 阿里云 OSS（AWS 用 S3，腾讯云用 COS）
    bucket = "terraform-state-prod"
    prefix = "myapp/prod"
    key    = "terraform.tfstate"
    # state 文件含敏感信息（密码/密钥），必须：
    # 1. 存远程对象存储（不存 Git）
    # 2. 开加密
    # 3. 开版本控制（误删可恢复）
    # 4. 限制访问权限
  }
}
```

> **state 文件铁律**：
> 1. **绝不存 Git 仓库**——state 文件含明文密码，泄露 = 灾难
> 2. **必须存远程**（S3/OSS/COS）+ 加密 + 版本控制
> 3. **绝不手动改 state**——用 `terraform import` 导入已有资源，不要手编辑
> 4. **CI 里用 `terraform plan` 预览变更，人工 Review 后 `terraform apply`**

**模块化**（复用 Terraform 代码）：

```
terraform/
├── modules/
│   ├── web-server/          # 可复用的 Web 服务器模块
│   │   ├── main.tf
│   │   ├── variables.tf
│   │   └── outputs.tf
│   ├── rds/                 # 数据库模块
│   └── vpc/                 # 网络模块
├── environments/
│   ├── dev/                 # dev 环境引用模块
│   │   └── main.tf
│   ├── stage/
│   └── prod/
```

```hcl
# environments/prod/main.tf
module "myapp_server" {
  source = "../../modules/web-server"

  instance_type = "ecs.g6.large"
  env           = "prod"
  service_name  = "myapp"
  instance_count = 3           # prod 跑 3 台
}
```

### 10.3.3 IaC + CI/CD：基础设施变更也走 Pipeline

**核心理念**：基础设施变更和代码变更一样走 CI/CD——`terraform plan` 在 CI 里跑（预览变更），`terraform apply` 需要人工确认（防止误操作）。

```yaml
# .gitlab-ci.yml（IaC 流水线，和业务代码流水线分离）
stages:
  - validate
  - plan
  - apply

# === 1. 语法校验 ===
terraform:validate:
  stage: validate
  image: hashicorp/terraform:latest
  script:
    - cd terraform/environments/$TF_ENV
    - terraform init -backend=false
    - terraform fmt -check        # 格式检查
    - terraform validate          # 语法校验
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes: ["terraform/**/*"]

# === 2. Plan（预览变更，MR 里展示） ===
terraform:plan:
  stage: plan
  image: hashicorp/terraform:latest
  script:
    - cd terraform/environments/$TF_ENV
    - terraform init
    - terraform plan -out=tfplan      # 生成 plan 文件
    - terraform show -json tfplan > tfplan.json
  artifacts:
    paths:
      - tfplan
      - tfplan.json
    reports:
      terraform: tfplan.json          # GitLab MR 里展示 Terraform 变更预览
  rules:
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
      changes: ["terraform/**/*"]

# === 3. Apply（人工确认后执行） ===
terraform:apply:
  stage: apply
  image: hashicorp/terraform:latest
  needs: ["terraform:plan"]
  script:
    - cd terraform/environments/$TF_ENV
    - terraform init
    - terraform apply -auto-approve tfplan
  rules:
    - if: $CI_COMMIT_BRANCH == "main"
      changes: ["terraform/**/*"]
      when: manual        # 人工确认才执行，不自动 apply
  environment:
    name: $TF_ENV
```

> **IaC CI/CD 铁律**：
> 1. **plan 自动跑，apply 人工确认**——`terraform apply` 会改真实基础设施，必须人工把关
> 2. **绝不手动改云资源**——所有变更必须走 Terraform MR，否则 state 会 drift（和实际不一致）
> 3. **prod 环境 apply 需要两人 Review**——Terraform MR 的 Approve 规则设 ≥ 2 人
> 4. **state 文件上锁**——用 Terraform Cloud / Atlantis 防止并发 apply 导致 state 冲突

---

## 10.4 DORA 指标度量与持续改进

### 10.4.1 DORA 四指标回顾与采集落地（衔接 S3 + 09 章）

> DORA 四指标的完整定义和采集方法参见 [S3 DORA 四指标](./补充专题/S3-DORA四指标.md)，本节聚焦**怎么在 CI/CD 里落地采集**。

**图 10-1：DORA 指标采集架构**

```
┌─────────────┐     ┌──────────────┐     ┌───────────────┐
│  GitLab CI  │     │  Prometheus  │     │   Grafana     │
│             │     │              │     │               │
│ Deploy Job  │────→│ deploy_total │────→│ DORA 仪表盘   │
│ 成功后埋点   │     │ deploy_failed│     │               │
│             │     │ lead_time    │     │ 4 个 Panel:   │
│ Git commit  │     │ mttr         │     │ - 部署频率    │
│ 时间戳      │────→│              │     │ - 前置时间    │
│             │     │ Alertmanager │     │ - 失败率      │
│             │     │ 故障/恢复    │────→│ - MTTR        │
└─────────────┘     └──────────────┘     └───────────────┘
```

**采集实现**（在 GitLab CI Deploy Job 里埋点）：

```yaml
# .gitlab-ci.yml（Deploy 成功后埋点 DORA 指标）
deploy:prod:
  stage: deploy
  # ... 部署逻辑 ...
  after_script:
    # 1. 记录部署次数（部署频率）
    - |
      curl -X POST http://pushgateway:9091/metrics/job/deploy \
        -d "deploy_total{env=\"prod\",service=\"myapp\",version=\"$CI_COMMIT_TAG\"} 1"
    # 2. 记录变更前置时间（commit → 部署 的时长）
    - |
      COMMIT_TIME=$(git show -s --format=%ct $CI_COMMIT_SHA)
      NOW=$(date +%s)
      LEAD_TIME=$(( (NOW - COMMIT_TIME) / 60 ))  # 分钟
      curl -X POST http://pushgateway:9091/metrics/job/deploy \
        -d "lead_time_minutes{env=\"prod\",service=\"myapp\"} $LEAD_TIME"
  rules:
    - if: $CI_COMMIT_TAG
```

**DORA 仪表盘 PromQL**（在 Grafana 里配 4 个 Panel）：

| Panel | PromQL | Elite 基线 |
| ----- | ------ | ---------- |
| 部署频率 | `sum(rate(deploy_total{env="prod"}[1d]))` | ≥ 1 次/天 |
| 变更前置时间 | `histogram_quantile(0.5, lead_time_minutes_bucket)` | < 60 分钟 |
| 变更失败率 | `sum(rate(deploy_failed_total[30d])) / sum(rate(deploy_total[30d]))` | < 5% |
| MTTR | `histogram_quantile(0.5, mttr_minutes_bucket)` | < 60 分钟 |

### 10.4.2 从指标到行动：Low → Medium → High → Elite 晋升路径

> 参见 [S3 五、从 Low 到 Elite 的提升路径](./补充专题/S3-DORA四指标.md)，本节补充**结合 P1.3 的具体行动项**。

**表 10-4：DORA 晋升路径 × P1.3 对应行动**

| 阶段 | DORA 目标 | P1.3 对应行动 | 预计周期 |
| ---- | --------- | ------------- | -------- |
| **Low → Medium** | 月部署 → 周部署 | P1.3 四段门禁跑通 + dev/test 自动部署 | 1~3 个月 |
| **Medium → High** | 周部署 → 日部署 | Feature Flag 解耦 + stage 自动部署 + 监控告警 | 3~6 个月 |
| **High → Elite** | 日部署 → 按需多次 | 金丝雀 + 自动回滚 + SLO + 错误预算 | 6~12 个月 |

**Low → Medium 的 3 个关键行动**（最容易见效）：
1. **CI 流水线跑通**：PR 必须过 Lint + Test + 安全扫描（08 章）
2. **dev/test 自动部署**：merge dev 自动部署到 dev 环境，不用人工 SSH（07 章 7.6）
3. **prod 人工 Tag 触发**：P1.3 两段式机制，打 Tag 才部署 prod（07 章 7.4.4）

**Medium → High 的 3 个关键行动**：
1. **Feature Flag 解耦**：部署 ≠ 发布，代码先上线（Flag 关），产品按需开 Flag（07 章 7.5）
2. **可观测性到位**：Prometheus + Grafana + Loki 三支柱（09 章 9.1~9.4）
3. **告警分级 + OnCall**：P0~P3 分级，MTTR 从"发现不了"到"5 分钟定位"（09 章 9.7）

**High → Elite 的 3 个关键行动**：
1. **渐进式发布**：金丝雀 / 蓝绿自动切流（07 章 7.2）
2. **自动回滚**：Prometheus 指标异常自动触发回滚（09 章 9.8.4）
3. **SLO + 错误预算**：数据化驱动发布决策（09 章 9.6）

### 10.4.3 CI/CD 成熟度自评清单（20 项打分表）

> 对照以下 20 项自评，每项 0~2 分（0=没做，1=做了但不完善，2=完善）。满分 40 分。

**表 10-5：CI/CD 成熟度自评清单**

| # | 维度 | 评估项 | 0 分 | 1 分 | 2 分 |
| - | ---- | ------ | ---- | ---- | ---- |
| 1 | 版本控制 | 代码在 Git 仓库 | 手动传文件 | 在 Git | Git + MR Review |
| 2 | 版本控制 | 分支策略 | 没有 | 有但不规范 | P1.3 四段门禁 |
| 3 | CI | PR 自动触发 CI | 没有 | 有 | 有 + 阻断合并 |
| 4 | CI | Lint + 单元测试 | 没有 | 有 | 有 + 覆盖率门禁 |
| 5 | CI | 安全扫描（SAST/SCA/Secret） | 没有 | 有但软告警 | 有 + 硬阻断 |
| 6 | 构建 | 镜像构建 | 手动 docker build | CI 自动 build | Kaniko + Cosign 签名 |
| 7 | 制品 | 镜像仓库 | Docker Hub | 私有仓库 | 私有 + 漏洞扫描 + SBOM |
| 8 | 部署 | 部署方式 | SSH 手动部署 | CI 自动部署 | GitOps / IaC |
| 9 | 部署 | 发布策略 | 直接替换 | 滚动发布 | 金丝雀 / 蓝绿 |
| 10 | 部署 | 环境管理 | 手动配 | 有 dev/prod | dev/test/stage/prod 四段 |
| 11 | 安全 | 密钥管理 | 硬编码 | CI 变量 | Vault / OIDC |
| 12 | 安全 | 供应链安全 | 没有 | Trivy 扫镜像 | SBOM + Cosign + DT 持续监控 |
| 13 | 监控 | 指标采集 | 没有 | 基础 CPU/QPS | RED + USE + SLO |
| 14 | 监控 | 日志聚合 | SSH grep | ELK/Loki | 结构化日志 + Loki + LogQL |
| 15 | 监控 | 链路追踪 | 没有 | 有但不用 | OTel + Tempo + 三支柱联动 |
| 16 | 告警 | 告警分级 | 全发飞书 | 有分级 | P0~P3 + OnCall + runbook |
| 17 | 回滚 | 回滚能力 | 没有 | 手动回滚 | 自动回滚 + 决策树 |
| 18 | 度量 | DORA 指标 | 不知道 | 知道没采 | 采集 + 仪表盘 + 持续改进 |
| 19 | 复用 | 流水线复用 | 每仓复制粘贴 | 有公共模板 | ci-templates 仓 + 版本化 |
| 20 | IaC | 基础设施 | 手动建 | 部分脚本化 | Terraform + CI/CD + state 管理 |

**评分对照**：

| 分数 | 等级 | 对应 DORA | 下一步重点 |
| ---- | ---- | --------- | ---------- |
| 0~13 | 初级 | Low | 先跑通 CI + 自动部署 |
| 14~26 | 中级 | Medium | 加监控 + Feature Flag + 安全门禁 |
| 27~34 | 高级 | High | 加金丝雀 + 自动回滚 + SLO |
| 35~40 | 精英 | Elite | 持续优化 + Monorepo + IaC |

---

## 10.5 P1.3 全链路回顾与演进路线

### 10.5.1 01~09 章知识点 × P1.3 实践对照全表

> 这是全书的"知识地图"——每个章节学了什么，在 P1.3 实践中对应哪个环节。

**表 10-6：全书知识点 × P1.3 对照**

| 章节 | 知识点 | P1.3 中的位置 | 现在做了吗 |
| ---- | ------ | ------------- | ---------- |
| 01 认知地基 | CI/CD/CD 概念 + 黄金八段 | 整体框架 | ✅ |
| 02 环境与前置 | Git 工作流 + Docker + YAML | feature 分支 + docker compose | ✅ |
| 03 工具选型 | GitLab CI 选型 | 全流程用 GitLab CI | ✅ |
| 04 核心工具 | GitLab CI 语法 | .gitlab-ci.yml 编写 | ✅ |
| 05 构建与测试 | pytest + 覆盖率门禁 | feature MR 阶段跑 | ✅ |
| 06 容器化 | Kaniko + Trivy + Cosign + Harbor | test 段开始构建镜像 + 扫描 | ✅ |
| 07 部署与发布 | 三种发布策略 + K8s/Compose + Feature Flag + GitOps | stage/main 段部署 | ✅ |
| 07 P1.3 门禁 | dev/test/stage/main 四段触发 + 两段式 main | 核心机制 | ✅ |
| 07 踩坑录 | 7.9 六类事故复盘 | 全程参考 | ✅ |
| 08 安全门禁 | SAST + SCA + Secret + DAST + 质量门 | feature MR + 各段扫描 | ✅ |
| 08 P1.3 落地 | 四段环境 × 安全扫描矩阵 | 8.6.1 表格 | ✅ |
| 09 可观测性 | Prometheus + Loki + OTel + SLO + 告警 + 回滚 | test 起步 → prod 全量 | ✅ |
| 09 P1.3 落地 | 四段环境 × 监控配比矩阵 | 9.9.1 表格 | ✅ |
| 10 流水线复用 | ci-templates 仓 + include | 下一步优化 | ⏳ |
| 10 Monorepo | 变更检测 + 增量构建 | 服务 > 5 个再考虑 | ⏳ |
| 10 IaC | Terraform + CI/CD | 基础设施代码化 | ⏳ |
| 10 DORA | 四指标采集 + 成熟度自评 | 度量 + 持续改进 | ⏳ |

### 10.5.2 小团队 6 个月演进路线图（从起步到 Elite）

**图 10-2：6 个月演进路线**

```
月份 1~2：跑通基础闭环（Low → Medium）
├─ GitLab CI + feature 分支 + P1.3 四段门禁
├─ PR 自动跑 Lint + 单元测试
├─ dev/test 自动部署（docker compose）
├─ prod 打 Tag 手动触发
└─ Secret Detection（< 30 秒，第一个安全扫描）

月份 3~4：加安全 + 监控（Medium → High）
├─ SAST（Bandit + Semgrep）+ SCA（pip-audit）
├─ Kaniko 构建镜像 + Trivy 扫描 + Cosign 签名（06 章）
├─ Prometheus + Grafana + Loki 监控栈跑起来（09 章）
├─ RED 三指标埋点 + 基础仪表盘
├─ 告警分级 P0~P3 + 飞书通知
└─ Feature Flag 自建最小版（ConfigMap + 热更新）

月份 5~6：加可观测性 + 自动化（High → Elite）
├─ OTel 链路追踪 + Tempo 后端
├─ SLO 定义 + 错误预算 + 发布冻结联动
├─ 金丝雀发布或蓝绿发布（07 章）
├─ 自动回滚（Prometheus 异常 → 触发回滚）
├─ DORA 指标采集 + 仪表盘
├─ ci-templates 仓（流水线复用）
└─ Terraform IaC（基础设施代码化）
```

**每个里程碑的"完成标志"**：

| 里程碑 | 完成标志 | DORA 级别 |
| ------ | -------- | ---------- |
| 月份 2 结束 | PR 必须过 CI 才能合并 + dev/test 自动部署 + prod Tag 触发 | Medium |
| 月份 4 结束 | 安全扫描四类全配 + 监控三支柱跑通 + 告警分级 + Feature Flag | High |
| 月份 6 结束 | SLO + 错误预算 + 自动回滚 + DORA 仪表盘 + IaC | Elite |

> **演进的核心原则**：**不要跳步**。Month 1 的基础闭环没跑稳就上 Month 5 的自动回滚，等于在沙地上建高楼。每一步都验证"上一个能力稳定运行 2 周以上"再进下一步。

---

## 10.6 生产常见问题与事故复盘

### 10.6.1 ci-templates 仓改了模板导致所有项目 CI 一起挂（模板管理事故）

**事故**：ci-templates 仓的维护者在 `fastapi-base.yml` 里改了 `before_script` 的 pip 安装命令，没通知引用方。所有用 `ref: 'main'` 引用的项目（15 个服务）CI 一起挂——pip install 报错。因为模板引用没锁版本，main 一改全部生效。

**根因**：
1. 模板引用用了 `ref: 'main'` 而不是 `ref: 'v2.1.0'`
2. 模板变更没有灰度过程（没有先在 1 个项目验证）

**修复**：
1. **所有引用必须锁版本号** `ref: 'vX.Y.Z'`，禁止用 `ref: 'main'`
2. **模板变更走 MR + Code Review**，MR 描述里写明"会影响哪些引用方"
3. **Breaking Change 先发 `vX.Y.0-rc.1` 预发布版**，让 1~2 个项目先试用，稳定后再发正式版
4. **CI 里加检查**：扫描所有项目 `.gitlab-ci.yml`，如果有 `ref: 'main'` 就告警

### 10.6.2 Monorepo 变更检测漏判 → 改了 shared 没触发下游 CI（漏构建事故）

**事故**：Monorepo 里 `shared/proto/order.proto` 改了字段，但变更检测脚本只检查了 `services/*/` 目录，没检测 `shared/` → order-service 没重建 → 部署后旧镜像用旧 proto 反序列化新数据 → 线上反序列化错误。

**根因**：变更检测脚本没有覆盖 shared → 下游的依赖关系。

**修复**：
1. **变更检测必须包含 shared 目录**：`shared/` 变了 → 所有依赖 shared 的服务都触发 CI
2. **用依赖图工具**（如 Bazel query / nx affected）自动计算影响范围，不靠手动维护
3. **CI 里加一致性检查**：shared 变了但下游服务没重建 → CI 失败

### 10.6.3 Terraform state 文件误删 → 所有基础设施"消失"（IaC 灾难事故）

**事故**：运维清理 OSS 存储桶时误删了 `terraform-state-prod/` 目录。Terraform state 文件丢了 → `terraform plan` 认为所有资源都不存在（因为 state 是空的）→ `terraform apply` 会尝试创建所有资源 → 但资源实际还在（云上）→ 冲突报错。恢复花了 3 小时（从 OSS 版本控制里找回 state 文件）。

**根因**：
1. state 文件存 OSS 但没开版本控制
2. 清理脚本没有白名单保护

**修复**：
1. **state 文件存储必须开版本控制**（OSS 版本控制 / S3 Versioning）——误删可以恢复
2. **state 文件存储桶加生命周期保护**（MFA Delete / 软删除）
3. **定期 `terraform plan` 检查 state drift**——state 和实际不一致时提前发现
4. **`terraform import` 批量导入**——如果 state 真丢了，用 import 把云上已有资源重新导入 state

### 10.6.4 DORA 指标采集后没人看 → 度量变成摆设（管理事故）

**事故**：团队花 2 周搭了 DORA 仪表盘，数据采集正常，但没有人定期看。3 个月后部署失败率从 5% 涨到 15%，MTTR 从 30 分钟涨到 2 小时——仪表盘上都有数据，但没人发现，直到客户投诉。

**根因**：
1. 仪表盘没有告警——指标恶化不会主动通知
2. 没有"度量回顾会"——数据采了不看等于没采

**修复**：
1. **DORA 指标加告警**：失败率 > 10% → P1 告警；MTTR > 1 小时 → P1 告警
2. **每月度量回顾会**：团队每月看一次 DORA 仪表盘，讨论"哪里退步了、怎么改进"
3. **把 DORA 指标和团队目标绑定**：季度 OKR 里写"部署频率 > 3 次/周 + 失败率 < 5%"
4. **仪表盘放在显眼位置**：Grafana 大屏投到办公区电视上，让所有人随时看到

---

## 10.7 全书小结与自检（15 个必答问题）

> 这是全书的最终自检，覆盖 01~10 章核心知识点。能答清楚 12 个以上 = 掌握了 CI/CD 体系。

### 10.7.1 必须能答出来的 15 个问题

**基础概念（01~03 章）**：
1. CI、CD（持续交付）、CD（持续部署）的区别是什么？你的 P1.3 属于哪一种？
2. 黄金八段是哪八段？你的 P1.3 覆盖了哪几段？
3. GitLab CI 为什么要选它而不是 GitHub Actions / Jenkins？

**构建与测试（04~06 章）**：
4. pytest 覆盖率门禁为什么只看 New Code 不看 Overall Code？
5. Kaniko 相比 DinD 的优势是什么？为什么不需要 Docker daemon？
6. Trivy 镜像扫描和 pip-audit 代码层扫描的区别是什么？为什么要两个都配？
7. Cosign 签名解决了什么安全问题？验签在 Pipeline 的哪个环节做？

**部署与发布（07 章）**：
8. P1.3 中 feature 分支贯穿所有环境（不做逐级 merge）的好处是什么？
9. Feature Flag 四层方案（Nginx/Gateway/应用/Unleash）的侵入度递增逻辑是什么？
10. Expand-Contract 三步法为什么 Step 1 和 Step 3 必须相隔 1~2 个版本周期？

**安全与质量（08 章）**：
11. 安全左移的收益量化是多少？为什么密钥泄露后第一步是轮换密钥而不是删 Git 历史？
12. SAST/SCA/Secret/DAST 四类扫描分别扫什么？为什么互不替代？

**监控与回滚（09 章）**：
13. 可观测性三支柱（Metrics/Logs/Traces）分别回答什么问题？出问题时的标准排查路径是什么？
14. 回滚决策树的四个判断点是什么？数据库回滚为什么可能回滚不了？

**工程化与度量（10 章）**：
15. DORA 四指标是什么？你的团队现在在 Low/Medium/High/Elite 哪个级别？下一步怎么晋升？

### 10.7.2 全书知识点速记（一张图）

```
                    CI/CD 全链路（黄金八段 + P1.3 落地）
                    ═══════════════════════════════════

 ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐
 │  Lint   │→│  Build  │→│  Test   │→│Security │→│ Package │→│ Publish │→│ Deploy  │→│ Verify  │
 │ (04,05) │  │ (05,06) │  │ (05,08) │  │  (08)   │  │ (06)    │  │ (06)    │  │ (07)    │  │ (09)    │
 └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └─────────┘
      ↓              ↓           ↓            ↓            ↓            ↓            ↓            ↓
   ruff/flake8   Kaniko     pytest      SAST/SCA/    Docker       Harbor/      K8s/Compose   Prometheus
   mypy          镜像构建    coverage    Secret/      image        Registry     helm/argocd   Grafana
                            门禁        DAST                                              Loki/OTel
                                                                                               ↓
                                                                            ┌────────────────────────┐
                                                                            │  P1.3 四段门禁          │
                                                                            │  feature → dev → test  │
                                                                            │  → stage → main → Tag  │
                                                                            └────────────────────────┘
                                                                                               ↓
                                                                            ┌────────────────────────┐
                                                                            │  10.5 工程化度量        │
                                                                            │  DORA 四指标 + 成熟度   │
                                                                            │  ci-templates + IaC    │
                                                                            └────────────────────────┘
```

### 10.7.3 全书章节关联补充专题索引

| 专题 | 主要关联章节 |
| ---- | ------------ |
| [S1 Secret 管理](./补充专题/S1-Secret管理.md) | 06、08 |
| [S2 成本与配额](./补充专题/S2-成本与配额.md) | 03、04、10 |
| [S3 DORA 四指标](./补充专题/S3-DORA四指标.md) | 09、10 |
| [S4 供应链安全 SBOM](./补充专题/S4-供应链安全SBOM.md) | 06、08 |
| [S5 流水线自身安全](./补充专题/S5-流水线自身安全.md) | 04、08 |
| [S6 数据库迁移](./补充专题/S6-数据库迁移.md) | 03、04、08 |
| [S7 Feature Flag 解耦](./补充专题/S7-FeatureFlag解耦部署.md) | 07 |
| [S8 Lua 场景 CI/CD](./补充专题/S8-Lua场景CICD.md) | 05 |
| [S9 质量安全扫描集成](./补充专题/S9-质量安全扫描集成.md) | 05、06、08 |
| [S10 Pipeline 最佳实践](./补充专题/S10-Pipeline各环节最佳实践.md) | 04、05、06 |
| [S11 包仓库 Nexus/Harbor/GitLab](./补充专题/S11-包仓库Nexus-Harbor-GitLabRegistry.md) | 02、06 |

---

## 全书完

恭喜读完 10 章 CI/CD 系统化学习路径。从 01 章的"CI/CD/CD 概念区分"到 10 章的"DORA 指标度量与持续改进"，你已经掌握了：

- **理论框架**：黄金八段 + P1.3 四段门禁 + 三类门禁
- **工具链**：GitLab CI + Kaniko + Trivy + Cosign + Harbor + Argo CD + Prometheus + Loki + OTel + Grafana
- **安全体系**：SAST + SCA + Secret Detection + DAST + 供应链安全
- **可观测性**：Metrics + Logs + Traces + SLO + 告警 + 回滚
- **工程化**：流水线复用 + Monorepo + IaC + DORA 度量

**下一步建议**：
1. 用 10.4.3 的 20 项自评清单评估当前团队水平
2. 按 10.5.2 的 6 个月路线图制定改进计划
3. 每月回顾 DORA 指标，持续改进
4. 把学到的知识用 [我的实践记录.md](./我的实践记录.md) 和实际项目对照，不断迭代

CI/CD 不是"学完就完了"的技能，而是**持续演进的工程实践**。祝你的团队从 Low 一路晋升到 Elite。
